/**
 * Represents a component with no data but an onClick handler.
 */
export interface NoData {
    /**
     * The function to be called when the component is clicked.
     */
    onClick: () => void;
}
