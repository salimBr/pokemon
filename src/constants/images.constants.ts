/**
 * Object containing image assets used in the application.
 * @property {string} emptyPokeball - Image asset for an empty Pokeball.
 * @property {string} pokeball - Image asset for a Pokeball.
 */
const Images = {
    emptyPokeball: require("../assets/images/empty-pokeball.png"),
    pokeball: require("../assets/images/pokeball.png")
}

export default Images;